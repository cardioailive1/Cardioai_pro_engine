fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydicom==2.4.4
python-multipart==0.0.9
websockets==13.1
httpx==0.28.1   # real live dependency — integrations/iomt_client.py's outbound calls to the IoMT backend
numpy==2.1.3   # real live dependency — integrations/dicom.py (pixel feature extraction) and longitudinal/trend_engine.py both import it directly

# scikit-learn, torch, and torchvision live in requirements-training.txt,
# not here — they're needed to run training/ and imaging_models/ locally
# or in a Jupyter Workspace, but NOT imported anywhere in the live web
# service's actual request path. Keeping them out of this file is what
# keeps the Render deploy build fast and the image small; installing them
# here would add several minutes and multiple GB to every deploy for zero
# production benefit. Run `pip install -r requirements-training.txt` in
# whatever environment you actually run the training scripts from.
